import random
import math
numbers =[ random.randint(1,200) for i in range(20)]
print("Numbers:",numbers)

max_val=max(numbers)
min_value=min(numbers)
print("Maximun_value:",max_val)
print("Minimun value",min_value)